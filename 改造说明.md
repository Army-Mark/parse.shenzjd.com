# 自建改造说明：剥离第三方认证 · 品牌替换 · 上游同步防覆盖

> 适用对象：从 `wu529778790/parse.shenzjd.com` fork 后进行自建部署的实例。
> 改造目标：① 解除"必须扫码登录作者小程序/公众号"的限制；② 把品牌与域名换成本站信息；
> ③ **保留上游同步能力，同时保证改造不会被上游改动覆盖回去**。

---

## 一、为什么必须改

原项目把认证实现为**远程 SaaS 依赖**，仓库内不含账号体系：

- 服务端把每个解析请求的凭证转发到 `https://wx-auth.shenzjd.com/api/auth/check` 做权威校验，未通过返回 401，且为 fail-closed（对方服务不可用即本站全站解析不可用）。
- 前端从 unpkg 加载第三方脚本 `wx-auth-sdk`，其内置默认 `apiBase` 即作者服务器；登录弹窗的「小程序登录」调用 `/api/auth/mp-scan/create` 生成二维码，扫的是作者小程序的码；「验证码」方式则要求关注作者公众号。
- 该 SDK 会以 `document.cookie` 把 `wxauth-token` / `wxauth-openid` 写入**部署方自己域名的根域**，属于第三方脚本持有本站凭证写入能力。

> 作者虽已开源后端 `wu529778790/wx-auth`（Nuxt 4，MIT），但核对文件树后确认**不含 `mp-scan` 路由**——小程序扫码登录未开源，自建最多只能拿到"公众号验证码"方式。因此选择彻底去认证。

---

## 二、变更清单

### 认证核心（移除）

| 文件 | 变更 |
| --- | --- |
| `src/lib/api-middleware.ts` | 删除 `AUTH_REQUIRED_ROUTES` 常量与整个认证门禁分支；移除 `wx-auth-guard` 导入。解析接口不再返回 401 |
| `src/components/VideoParserForm.tsx` | 删除 `showWxAuth()` 调用与导入；顺带清理已失效的广告弹窗注释代码 |
| `src/lib/wx-auth-client.ts` | **已删除**（前端第三方 SDK 加载器） |
| `src/lib/wx-auth-guard.ts` | **已删除**（服务端远程校验守卫） |
| `src/lib/floating-unlock-client.ts` | **已删除**（依赖作者小程序广告的解锁封装） |
| `src/config/floating-unlock.ts` | **已删除** |
| `tests/wx-auth-guard.test.ts` | **已删除**（其断言已与目标行为相反） |

### 第三方前端资源（移除）

| 文件 | 变更 |
| --- | --- |
| `src/app/layout.tsx` | 移除作者托管在 unpkg 的 `@wu529778790/site-navbar`（内部自动初始化其登录 SDK）与 `@wu529778790/floating-qr`（公众号浮窗）；移除 `<site-navbar />` 元素；图标改为本站 `public/brand/` |
| `src/app/globals.css` | 移除 `site-navbar` 样式块 |
| `src/types/custom-elements.d.ts` | **已删除**（已无自定义元素需要声明） |

### 连带修复（不改会出问题）

| 文件 | 问题 | 修复 |
| --- | --- | --- |
| `src/app/api/stats/route.js` | 原依赖 `getWxAuthUser`，守卫删除后会直接报错 | 改用 `Authorization: Bearer <STATS_API_KEY>`，未配置时返回 503 |
| `src/lib/api-utils.js` | CORS 白名单硬编码 `*.shenzjd.com`，自建域名跨域调用会被浏览器拦截 | 改由环境变量 `ALLOWED_ORIGINS` 控制，未配置时不放行跨域（同源不受影响） |
| `src/lib/result-cache.js` | Cache API 的 key 硬编码作者域名，与作者同账户时缓存可能串味 | key 由 `NEXT_PUBLIC_SITE_URL` 派生，隔离各部署实例 |
| `src/config/site.ts` | 品牌名与域名硬编码为作者信息 | 改为 `NEXT_PUBLIC_*` 环境变量优先，默认值即本站信息 |
| `src/lib/honeypot.ts` | 蜜罐文案用于把爬虫流量引流到作者公众号 | 改为中性文案，品牌名取本站配置 |
| `public/manifest.webmanifest` | 名称与图标指向作者品牌及其 CDN | 改为通用名称 + 本站 `public/brand/` 图标 |
| `public/robots.txt`、`public/sitemap.xml` | 硬编码作者域名，且与已存在的 `app/robots.ts`、`app/sitemap.ts` 重复冲突 | **已删除**（动态路由会按 `NEXT_PUBLIC_SITE_URL` 生成） |
| `.github/workflows/sync-upstream.yml` | ⚠️ **每天 03:00 自动把上游作者仓库合并进 fork，规则是"除 `.github/` 外一律采纳上游"，会在 24 小时内把改造全部覆盖回去** | **已删除**，并换成第三方的自保护同步工作流 `selfhost-sync.yml`（见第五节） |
| `.github/workflows/deploy-to-docker.yaml` | 部署脚本硬编码作者服务器站点目录与端口 | `ENV_FILE` / `DEPLOY_PORT` 改为读取仓库变量，带默认值 |
| `.github/workflows/selfhost-sync.yml` | — | **新增**：补丁栈重放式同步，自带守卫与构建校验 |

### 部署形态收敛（仅保留 Docker / 服务器）

原项目同时支持 Vercel、Cloudflare Workers 与 Docker 三条部署路径。本 fork 的部署目标**收敛为 Docker / 自建服务器**，Cloudflare 专用件全部移除：

| 条目 | 变更 |
| --- | --- |
| `wrangler.toml` | **已删除**（Workers 部署配置） |
| `open-next.config.ts` | **已删除**（OpenNext 的 Cloudflare 适配器配置） |
| `package.json` → `"build:cf"` | **已删除**（Cloudflare 专用构建命令） |
| `package.json` → devDep `@opennextjs/cloudflare` | **已删除**（连带 `wrangler`，本地/CI 安装量减少约 51MB、271 个包） |
| `.gitignore` | 移除 `/.open-next/`、`.wrangler/` 两条产物忽略规则 |
| `CLAUDE.md`、`API.md`、`src/app/api/config/route.js` | 订正"三目标部署 / `wrangler.toml [vars]` / `wrangler secret put`"等失效表述，改为说明宿主机 `.env` + `docker run --env-file` |

> 为什么可以放心删：`src/`、`tests/`、`scripts/` 对上述文件与依赖**零引用**（已核验），`next build` 与 `vitest` 均不依赖它们。
> 反而 **Docker 路径能力更强** —— `Dockerfile` 内装了 `yt-dlp` + `ffmpeg`，**TikTok 解析只有 Docker 部署支持**（Serverless 无此环境）。

**两项"看着像 Cloudflare、实则必须保留"的**：

- `next.config.mjs` 的 `output: "standalone"` —— `Dockerfile` 靠 `node server.js` 启动，删了镜像起不来。它只是**因** Cloudflare 而无图片优化器（`images.unoptimized: true`），但 standalone 本身是服务器部署的关键。
- `undici` 依赖 —— `src/lib/bilibili-fetch.js` 顶层 `import { ProxyAgent }`，B站解析强依赖，与部署平台无关。
  ⚠️ **它同时决定了运行时的 Node 版本下限（≥ 22.19.0）**，见 §2.1。

### 2.1 运行时要求：Node ≥ 22.19.0（踩坑记录）

`undici@8` 自身的 `engines` 为 `node >= 22.19.0`。它的 webidl 层把
`node:worker_threads` 的 `markAsUncloneable` 挂到 `Headers` / `Request` 等构造器上
（`webidl.util.markAsUncloneable(this)`）；该 API 在 Node 20 上**不存在**，于是被赋值为
`undefined`，构造第一个 `Headers` 时就抛：

```
TypeError: e.util.markAsUnCloneable is not a function
    at .next/server/chunks/1831.js
```

**实际症状（本次部署真实遇到）**：

| 现象 | 说明 |
| --- | --- |
| B 站解析固定 **HTTP 500** | `biliFetch()` 无条件走 undici，必然触发 |
| 抖音等平台**完全正常** | 不经 undici；`bilibili-fetch.js` 仅被 `bilibili-wbi.js` 与 `parsers/bilibili.js` 引用，影响面就这一个平台 |
| `/api/health`、首页、`/api/image`、`robots.txt` 全 200 | 服务本身健康，掩盖了故障范围 |
| `npm run build` / `npm test` **均不报错** | 错误只在运行时发生，构建期完全无感 |
| 错误信息里的 `util` 具有误导性 | 它来自 undici 自己的 `webidl.util` 对象，**不是** Node 的 `util` 模块（`require('util').markAsUncloneable` 在任何版本都不存在） |

**处置**：升运行时，不要降 undici（降级会偏离上游依赖、加剧锁文件冲突）。
`package.json` 已声明 `"engines": { "node": ">=22.19.0" }`，且守卫（§5.5）会断言该下限。

本次服务器实测的隔离升级方式（不替换系统 Node，可秒级回滚）：

```bash
curl -fsSL -O https://mirrors.cloud.tencent.com/nodejs-release/v22.23.2/node-v22.23.2-linux-x64.tar.xz
curl -fsSL -O https://mirrors.cloud.tencent.com/nodejs-release/v22.23.2/SHASUMS256.txt
sha256sum -c --ignore-missing SHASUMS256.txt
tar -xJf node-v22.23.2-linux-x64.tar.xz -C /opt && mv /opt/node-v22.23.2-linux-x64 /opt/node22

# 自检：应输出 function
/opt/node22/bin/node -e "console.log(typeof require('node:worker_threads').markAsUncloneable)"
```

随后把 systemd 单元的 `ExecStart` 改为 `/opt/node22/bin/node server.js`，
并在 `EnvironmentFile` 之后补一行 `Environment=PATH=/opt/node22/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin`，
再 `systemctl daemon-reload && systemctl restart parse`。

**两项行为差异（不是删文件，需知情）**：

- `src/lib/result-cache.js`：Node 下 `typeof caches === "undefined"` 恒成立，**Cloudflare Cache API 分支永不执行**，自动退化为进程内存 Map（上限 500 条）。单容器单进程无碍；若将来上 PM2 cluster 或多副本，缓存不共享、重启清零，届时需换成 Redis / SQLite。
- B站出口：`scf-bilibili-relay/`（腾讯云函数中继）与 `BILIBILI_RELAY_URL` / `BILIBILI_PROXY` **保留未动** —— B站对海外 IP 返回 -412，国内服务器可直连，海外服务器必须靠中继或代理。

### 测试与文档

- `tests/api-middleware.test.ts`：CORS 用例改为按 `ALLOWED_ORIGINS` 断言；新增"未配置时不放行任何跨域"用例；蜜罐用例改为按本站配置断言；**新增回归用例锁定"无任何凭证也能解析"**。
- `tests/share.test.ts`：把含作者 CDN 域名的夹具改成中性 URL（`cdn.example.com`），避免与"禁止第三方作者资源"的守卫规则冲突。
- `CLAUDE.md`、`API.md`、`docs/小程序接入抖音解析接口话术.md`：订正"需要微信认证"的过时描述，并留下"不要重新引入第三方认证调用"的约束说明。

---

## 三、品牌替换（已完成）

| 项目 | 原值 | 现值 |
| --- | --- | --- |
| 品牌名 | 神族九帝 | **口袋时光** |
| 域名 | shenzjd.com | **113826.xyz** |
| 站点地址 | https://parse.shenzjd.com | **https://parse.113826.xyz** |
| 联系 / 版权邮箱 | shenzujiudi@gmail.com | **已去除**（默认留空；法律页改渲染中性提示） |
| Cloudflare Worker 名 | parse-shenzjd-com | **parse-113826-xyz** |

**按需求保留上游原文、未做替换的部分**（见 `scripts/rebrand.mjs` 的 `KEEP_ORIGINAL`）：SEO 文案（`src/config/seo-platforms.ts`、`src/app/page.tsx`、`src/app/platform/`）、品牌 SVG（`public/brand/`）、`README.md`。

> ⚠️ 注意：以上保留文件对外仍会显示原作者品牌字样。若需要一并替换，从 `scripts/rebrand.mjs` 的 `KEEP_ORIGINAL` 中移除对应项后重跑脚本即可。

替换工具幂等可重复运行；代码注释中需要引用"上游原样信息"的行加了 `rebrand-keep` 标记予以豁免：

```bash
node scripts/rebrand.mjs --name "口袋时光" --domain 113826.xyz \
     --url "https://parse.113826.xyz"            # 预演
node scripts/rebrand.mjs ... --write               # 写入（不传 --email 即"去掉作者邮箱"）
```

---

## 四、新增环境变量

```bash
# 站点身份（NEXT_PUBLIC_* 为构建期注入，须在 build 前设置）
NEXT_PUBLIC_SITE_NAME=口袋时光
NEXT_PUBLIC_SITE_DOMAIN=113826.xyz
NEXT_PUBLIC_SITE_URL=https://parse.113826.xyz
NEXT_PUBLIC_CONTACT_EMAIL=          # 留空即不展示
NEXT_PUBLIC_COPYRIGHT_EMAIL=        # 留空即不展示

# 跨域放行（可选，逗号分隔，支持子域；不配则仅同源可用）
ALLOWED_ORIGINS=113826.xyz

# /api/stats 访问密钥（可选；不配则该接口关闭）
STATS_API_KEY=<openssl rand -hex 32>
```

`WXAUTH_API_BASE` 已废弃，可从部署环境删除。
`NEXT_PUBLIC_*` 有内置默认值，即使不配置也会显示"口袋时光 / 113826.xyz"，无需额外 `.env` 即可正确运行。

---

## 五、保留上游同步能力 + 防覆盖机制 ⭐

这是本次改造的核心设计。目标：**既能每日自动吸收上游新功能，又保证自建改造永远不被覆盖回去。**

### 5.1 思路：补丁栈重放（reset → reapply）

不再"在 fork 上就地合并上游"（那样必然被覆盖），而是把本地改造沉淀成一份**叠加层（overlay）**，每天：

```
① 重置工作区到上游最新 HEAD
② 把叠加层重新贴回去（补丁栈 + 删除清单）
③ 运行不变量守卫（selfhost-guard.mjs）
④ 按 package.json 重新生成锁文件（npm install --package-lock-only）
⑤ 跑 npm ci / npm test / npm run build
⑥ 全部通过才推送；任何一步失败即中止，线上仍运行上一个正常版本
```

叠加层由五部分组成：

| 路径 | 作用 |
| --- | --- |
| `selfhost/patches/*.patch` | 修改与新增的文件（`git apply --3way` 三方合并） |
| `selfhost/delete.txt` | 需要删除的路径清单 |
| `scripts/selfhost-guard.mjs` | 不变量守卫，最后一道防线 |
| `scripts/verify-overlay.mjs` | 等价性验证：证明叠加层能 1:1 重建改造结果（`npm run selfhost:verify`） |
| `.github/workflows/selfhost-sync.yml` | 每日定时执行的同步工作流（03:00 UTC） |

### 5.2 为什么删除要单独拆出来

删除**不写进补丁**，而是交给 `git rm -rf --ignore-unmatch`。原因：若把删除写进 patch，上游只要改动了这些文件（CI 工作流几乎天天改），`--3way` 就会冲突并让整次同步失败。用 `git rm --ignore-unmatch` 则对上游的任何改动都免疫。

### 5.3 为什么 `package-lock.json` 不进入补丁

锁文件是**派生产物，不参与合并**：

1. 上游几乎每次依赖升降级都会重写它，放进补丁会让 `--3way` 频繁冲突，而冲突意味着整次同步中止；
2. 它体量大 —— 本次仅"移除一个依赖子树"就产生 **966KB** 的 diff，且没有代码语义增量。

因此 `scripts/export-overlay.mjs` 把它列入 `DERIVED_PATHS` 排除出补丁；同步工作流在应用补丁后执行 `npm install --package-lock-only` 依据 `package.json` 重新生成锁文件，再由 `npm ci` 做同步校验，结果随提交一并落库。

> 若不做这一步：`package.json` 少了 `@opennextjs/cloudflare`、锁文件却还是上游版本，`npm ci` 会以"package.json 与 package-lock.json 不同步"直接失败。

### 5.4 每处本地改动都要"跑一遍导出"

改了源码 / 工作流 / 脚本之后，必须重新导出叠加层，否则同步任务用的还是旧补丁，新改动会被下游重置掉：

```bash
npm run selfhost:export     # = node scripts/export-overlay.mjs
npm run selfhost:guard      # = node scripts/selfhost-guard.mjs（可选，先自查）
npm run selfhost:verify     # = node scripts/verify-overlay.mjs（可选，验证等价性）

# 等价性验证在做什么：临时建一个指向上游基线（merge-base HEAD upstream/main）的
# worktree → 重放叠加层 → 与当前工作区比对目录树哈希。
# **归一化哈希两侧一致**才算通过（排除锁文件）。
# 它比"跑一次 CI 试试"快，也比肉眼 diff 可靠 —— 补丁漏收录某个文件时立刻报错。
```

`export-overlay.mjs` 会自动：把未跟踪文件登记进 diff、生成 `patches/0001-selfhost-overlay.patch`（排除删除）、生成 `delete.txt`（仅删除项）。**记得把 `selfhost/` 一起提交。**

### 5.5 守卫检查什么

补丁栈只能保证"改过的地方"被重放。如果上游在**别处**新增了对第三方认证的引用（新文件、新 import），补丁不会冲突、构建也可能通过，问题会静默上线。守卫把不变量显式断言出来：

- **禁止存在的文件**（10 项）：`wx-auth-client.ts`、`wx-auth-guard.ts`、`floating-unlock-client.ts`、`config/floating-unlock.ts`、`custom-elements.d.ts`、静态 `robots.txt` / `sitemap.xml`、`sync-upstream.yml`、`wrangler.toml`、`open-next.config.ts`
- **禁止出现的代码标记**（10 项，扫描 `src/` + `tests/`，先剥离注释避免误报）：`AUTH_REQUIRED_ROUTES`、`checkWxAuthToken`、`getWxAuthToken`、`getWxAuthUser`、`showWxAuth`、`unlockByAd`、`window.WxAuth`、`wx-auth-guard`、`wx-auth-client`、`opennextjs`
- **禁止出现的第三方资源**（5 项，仅扫 `src/` + `public/`，测试夹具除外）：`@wu529778790`、`cdn.jsdmirror.com`、`img.shenzjd.com`、`wx-auth.shenzjd.com`、`unpkg.com`
- **禁止写入指定文件**（2 项）：`package.json` 中不得再出现 `@opennextjs/cloudflare` 与 `build:cf`
- **运行时引擎下限**（1 项）：`package.json` 的 `engines.node` 必须 ≥ **22.19.0**。低于该下限时 `undici@8` 会在构造 `Headers` 时抛 `markAsUncloneable is not a function`（B 站解析 500），而**构建与单测都不会失败** —— 这类"只在运行时暴露"的问题必须在同步阶段拦下（详见 §2.1）
- **必须存在**（6 项）：`ALLOWED_ORIGINS`（api-utils）、`STATS_API_KEY`（stats 路由）、`NEXT_PUBLIC_SITE_URL`（site.ts + result-cache）、`NEXT_PUBLIC_CONTACT_EMAIL`（site.ts）、`selfhost` 工作流标记
- **必须存在的文件**：`selfhost/delete.txt`、`改造说明.md`

> 设计取舍：守卫只断言**有实质影响**的不变量。像 `.gitignore` 里的产物忽略规则这类纯装饰内容不纳入 —— 上游恢复它无害，不该因此让整次同步失败（避免误报阻断）。

### 5.6 冲突了怎么办

若上游改了你也改过的同一段代码，`git apply --3way` 会失败 → 同步任务中止（**不会**强推、不会静默退化，线上保持上一个正常版本）。处理方式：

```bash
git fetch upstream && git merge upstream/main     # 本地合并上游
# 手工解决冲突，确认改造仍然成立
npm run selfhost:guard && npm test && npm run build
npm run selfhost:export                           # 重新导出叠加层
git add -A && git commit -m "chore: resync upstream + refresh overlay" && git push
```

### 5.7 上游更新时怎么做（fork 模式）

本仓库是作者仓库的 fork，远端按惯例分两个角色：

| 远端 | 指向 | 权限 | 用途 |
| --- | --- | --- | --- |
| `origin` | `Army-Mark/parse.shenzjd.com`（你的 fork） | 可写 | 发布改造结果 |
| `upstream` | `wu529778790/parse.shenzjd.com`（作者） | 只读 | 上游更新来源 |

```bash
git remote set-url origin https://github.com/Army-Mark/parse.shenzjd.com.git
git remote add upstream https://github.com/wu529778790/parse.shenzjd.com.git
git fetch upstream
```

上游出新版本时有两条路径，**机制完全相同**（重置到上游 → 重放叠加层 → 守卫 → 测试 → 构建）：

1. **云端全自动（推荐）**：`.github/workflows/selfhost-sync.yml` 每天 03:00 UTC 自动执行，
   也可在 Actions 页面手动触发（`workflow_dispatch`）。无需任何本地操作。
2. **本地手动（发布前验证）**：

```bash
npm run selfhost:sync -- --check-only   # 先看上游有没有更新，不做改动
npm run selfhost:sync                   # 完整执行（默认只提交到本地分支）
npm run selfhost:sync -- --push         # 通过校验后再推送到 origin
```

> ⚠️ **不要**用 GitHub 的「Sync fork」按钮，也不要 `git merge --ff-only upstream/main`
> 之类的常规 fork 同步。本地 main 是「上游 + 改造提交」，与上游**永久处于分叉状态**：
> `--ff-only` 必然失败（每 6 小时一次红叉）；反过来，若它真能成功，就意味着改造被上游覆盖。
> 必须走上面「重置 + 重放」的路径。

---

## 六、验证结果（均已实测通过）

```bash
npm ci && npm test && npm run build
```

| 验证项 | 方式 | 结果 |
| --- | --- | --- |
| 单元测试 | `npm test` | **133 passed / 15 skipped**（skip 为需要真实外网的真机用例，未设置 `RUN_LIVE_PARSE`） |
| 生产构建 | `npm run build` | **通过**；全部路由正常生成，含 `○ /robots.txt`、`○ /sitemap.xml`（由 `app/` 路由生成，印证删除重复静态文件是对的） |
| **关键项：免登录解析** | 本地起服务后请求 `/api/parse?url=…`，**不带任何 Cookie / Authorization** | **HTTP 200 + 真实抖音视频数据** —— 认证门禁已彻底移除 |
| 统计接口鉴权 | 请求 `/api/stats`（未配置密钥） | **HTTP 503** + `统计接口未启用（未配置 STATS_API_KEY）` |
| 第三方脚本清除 | 抓取首页 HTML 检索 `unpkg.com` / `jsdmirror` / `site-navbar` / `floating-qr` | **零残留** |
| **叠加层等价性** | `npm run selfhost:verify`：临时建上游 HEAD 的 worktree → 重放补丁栈 → 执行删除清单 → 与工作区比对目录树哈希 | **两侧归一化 tree 哈希完全一致**。文件数 187/187、单侧独有 0、模式差异 0，内容差异**恰好 1 个且为设计上排除的 `package-lock.json`** —— 叠加层可 1:1 重建改造结果。该脚本可随时重跑自证（故此处不写死哈希值） |
| 补丁可移植性 | 检查 `selfhost/patches/*.patch` 的换行与覆盖面 | **纯 LF（0 个 CR 字节）**，在 Linux CI 上 `git apply` 无行尾风险；覆盖 31 个文件 |
| 增量移除后构建 | 删除 `wrangler.toml` / `open-next.config.ts` / `@opennextjs/cloudflare` 后重建 | **通过**，66 个静态页正常生成，无新增告警 |
| 依赖一致性 | `npm ci`（移除 271 个 CF 相关包后） | **同步校验通过**，无"package.json 与 lock 不同步"报错 |
| 守卫自查 | `npm run selfhost:guard` | **✓ 通过**（禁止文件 10 / 代码标记 10 / 资源域名 5 / 禁止写入 2 / 必需项 6 / 引擎下限 ≥22.19.0） |
| **守卫负向测试（一）** | 故意放回 `wrangler.toml` 并在 `package.json` 塞回 `@opennextjs/cloudflare` | **成功拦下 2 项违规并退出码 1** —— 证明新增检查确实生效，不是空转 |
| **守卫负向测试（二）** | 把 `engines.node` 由 `>=22.19.0` 改为 `>=20.9.0` | **退出码 1**，并给出「低于下限 22.19.0：undici@8 要求 … B 站解析运行时 500」的明确理由（详见 §2.1） |
| 品牌替换幂等性 | `rebrand.mjs` 连跑两次 | 第二次 **0 个文件变更** |
| **服务器实机：B 站解析** | 非 Docker 部署后请求 `/api/parse?url=<B站链接>` | **HTTP 200 + 真实视频数据**（0.39s）；完整链与 `b23.tv` 短链均通过 —— 修复了 §2.1 的 `markAsUncloneable` 500 |
| 服务器实机：端点回归 | `/api/health`、`/api/config`、`/api/image`、`/robots.txt`、`/sitemap.xml` | 全部 **200**；`/api/stats` **503**（未配密钥，符合设计）；`/api/bilibili` **403**（平台路由按设计仅内部可调） |
| 服务器实机：抖音 | `/api/parse?url=<抖音链接>`，不带任何凭据 | 请求成功抵达抖音 API 并返回**抖音服务端原生错误码**（测试视频已删）—— 免登录链路在生产环境成立 |
| 服务器实机：TikTok | 同接口，境内服务器 | 出网被墙（`curl: (28) 连接超时`），应用**优雅降级**返回结构化错误而非 500；**非应用缺陷**，如需支持需海外出口 |
| 服务器实机：运行时 | `systemctl show parse.service` | Node **22.23.2**（隔离安装于 `/opt/node22`），服务 `NRestarts=0`，常驻内存约 **95MB** |

---

## 七、安全提示（重要）

认证门禁移除后，解析接口变为**完全开放**。应用内已有防护仅剩：

- 基于 IP 的内存限流（60 次/分钟）
- IP 黑名单 + 蜜罐响应
- URL 合法性与 SSRF 校验

> ⚠️ 注意内存限流是**单进程内存**：多副本 / PM2 cluster 时各副本独立计数，重启即清零。多副本场景要么换 Redis 计数，要么把限流上移到网关。

服务器部署建议在**接入层**补一道防线：

1. **反向代理限流**：nginx `limit_req_zone` 对 `/api/parse` 限速（例如单 IP 10 r/s、突发 20），或 Caddy / Traefik 的速率限制中间件；配合 `limit_conn` 限制并发连接。
2. **封禁自动化**：`fail2ban` 或 nginx `deny` 规则，对高频 4xx/5xx 来源自动封禁，与应用层蜜罐形成互补。
3. **出网与资源**：解析需抓取上游页面，注意容器 CPU/内存上限与出网带宽；部署脚本已用 `--init` 启动容器以回收 `yt-dlp` 等子进程。
4. 若后续需要用户体系，**接入自建认证服务**，不要恢复对第三方认证服务的调用（该服务归原作者所有，其接口变更或停服会导致本站不可用）。

> 说明：删除 `wrangler.toml` 只影响"把应用跑在 Cloudflare Workers 上"这条路径。若将来想把 Cloudflare 当作**前置 CDN / WAF**（回源到你自己的服务器），不需要恢复这些文件，直接在 DNS 层接即可。

---

## 八、提交、部署与回滚

### 8.1 提交

本仓库**已完成首次推送**（`origin` = 你的 fork；所用凭据含 `workflow` 权限）：

```bash
git add -A
git commit -m "feat: self-host hardening — strip third-party auth, rebrand to 口袋时光, add overlay sync"
git push origin main          # origin = 你的 fork
```

> **迁到新账号时**：首次推送含 `.github/workflows/` 改动，需用带 `workflow` 权限的凭据；
> 之后到仓库 **Settings → Actions → General → Workflow permissions** 勾选
> **Read and write permissions**，否则 `selfhost-sync.yml` 无法推送。

启用 Actions 前请注意：

- GitHub 对 fork 默认**关闭** Actions，需在 **Actions** 页手动启用，
  `selfhost-sync.yml` 才会每天自动运行。
- `cleanup-actions.yaml` 每天清理 cache 与旧 run，无害，可保留。
- `deploy-to-docker.yaml` 已**默认关闭**：三个 job 都加了
  `if: vars.ENABLE_DOCKER_DEPLOY == 'true'`。原因见 §8.2 ——
  本服务器线上是 systemd，Docker 方案会抢同一个 3000 端口；且保留上游文件比
  删掉更省心（每次同步都会被「固定为上一版本」，删了也会被带回来）。

### 8.2 自动部署链路（上游更新 → 服务器，本次实际采用）

`selfhost-sync.yml` 里新增了 `jobs.deploy`，与 `jobs.sync` 用 `needs` 串联，
形成 **上游更新 → fork 仓库 → 服务器 systemd 服务** 的完整自动链路：

```
jobs.sync（见 §5.1）…… npm test → npm run build
    → 打包 standalone 产物（补 .next/static 与 public）→ 上传 artifact
    → 提交并推送（changed=true/false）

jobs.deploy（needs: sync，仅 changed=true 或手动 force_deploy 时执行）
    → 下载 artifact → SSH 上传到 DEPLOY_TMP_DIR
    → 解到暂存目录并校验 server.js / .next/static
    → 备份当前版本 → 原子替换 → chown → systemctl restart
    → 健康检查（20 次 × 3s，HTTP 200 通过）
        ├─ 通过 → 删除备份，任务成功
        └─ 失败 → 挪回上一版本并重启，打印 journalctl，任务失败
```

**为什么必须写在同一个工作流里**：用默认 `GITHUB_TOKEN` 提交的推送
**不会**触发其他工作流（GitHub 的反递归保护）。也就是说，如果指望
`deploy-to-docker.yaml` 监听 `push` 来部署，`sync` 推完代码后它根本不会启动。
要么同工作流内 `needs` 串联，要么用 PAT 推送 —— 这里选前者，少一个长期凭据。

**变更检测**：只有 `sync` 真的推了新提交才部署。上游没动就不做无谓的构建与重启
（`systemctl restart` 会让线上解析短暂中断）。需要强制重部署时，用
`workflow_dispatch` 的 `force_deploy` 开关（例如只在服务器上改了 `.env`）。

**需要的配置**：完整清单与一次性配置命令见
[`selfhost/README.md` → 自动部署](selfhost/README.md)。最少需要三个 Secret：
`DEPLOY_HOST`、`DEPLOY_USER`、`DEPLOY_SSH_KEY`（推荐专用用户 + 免密 sudo，
不要用 root 密码）。其余路径 / 服务名 / 健康检查地址都有内置默认值，
可用 Variables 覆盖。

> 安全取舍：`DEPLOY_SSH_KEY` 是私钥，务必用**专用部署密钥**并只装到该服务器，
> 服务器侧建议用 `command=` 或最小化 sudoers 收紧权限。当前仓库 Secrets 为空，
> 因此 `deploy` 任务会以「缺少部署配置」明确报错中止，而不是静默跳过。

### 8.3 备选：Docker 部署（默认已关闭）

若改用 Docker，推送 `main` 后由 `.github/workflows/deploy-to-docker.yaml` 完成。
**注意：需先在 Variables 里设 `ENABLE_DOCKER_DEPLOY = true` 才会运行**，
并先停用 `selfhost-sync.yml` 里的 `deploy` 任务，否则两边同时抢 3000 端口。

```
npm test → 构建 GHCR 镜像（ghcr.io/<你的仓库>:latest）→ SSH 到服务器
        → docker pull && docker stop/rm && docker run -d --init --env-file <ENV_FILE> -p <PORT>:3000
```

需要预先配置：

| 类型 | 名称 | 用途 |
| --- | --- | --- |
| Secret | `DEPLOY_HOST` / `DEPLOY_USER` / `DEPLOY_PASSWORD` | 目标服务器 SSH 凭据 |
| Variable | `DEPLOY_ENV_FILE` | 宿主机 `.env` 路径，默认 `/opt/parse/.env` |
| Variable | `DEPLOY_PORT` | 宿主机映射端口，默认 `3000` |

宿主机 `.env` 按 `.env.example` 填写（`NEXT_PUBLIC_*` 是**构建期**注入，需写进镜像构建环境或在容器内重新构建才能生效；其余如 `DOUYIN_COOKIE`、`STATS_API_KEY` 为运行期，放 `.env` 即可）。

想手动部署也可以直接用仓库里的 `Dockerfile`：

```bash
docker build --build-arg BUILD_ID=$(git rev-parse HEAD) -t parse:local .
docker run -d --init --env-file .env -p 3000:3000 parse:local
```

> `--build-arg BUILD_ID` 传稳定构建 ID，避免滚动更新时新旧容器的 Server Action / RSC 不一致。

### 8.3.1 无 Docker 的服务器部署（手工首次部署）

自动部署（§8.2）依赖服务器上已有的目录、服务与运行时，这些需要**先手工做一次**。
若目标服务器不便装 Docker，可直接跑 `output: "standalone"` 产物 + systemd。要点与命令：

```bash
# 0) 本地：务必先设好站点地址（NEXT_PUBLIC_* 是构建期注入，改完必须重建）
NEXT_PUBLIC_SITE_URL=http://<服务器IP>:3000 npm run build
#    产物在 .next/standalone/：含 server.js、.next/、public/、node_modules/

# 1) 打包上传（只传 standalone 三件套，约 24MB）
#    把 .next/static 复制进 standalone/.next/static，把 public/ 复制到 standalone/public/
tar -czf app.tar.gz -C .next/standalone .

# 2) 服务器：建非 root 用户与目录
useradd -r -s /usr/sbin/nologin parse
mkdir -p /opt/parse/app && tar -xzf app.tar.gz -C /opt/parse/app
chown -R parse:parse /opt/parse

# 3) 运行时依赖：Node ≥ 22.19（见 §2.1）+ TikTok 解析所需的 yt-dlp / ffmpeg
apt-get install -y ffmpeg && pip install -U yt-dlp

# 4) 环境变量
vim /opt/parse/.env      # 运行期变量；NODE_OPTIONS=--max-old-space-size=448 限制堆上限
chmod 600 /opt/parse/.env
```

systemd 单元（`/etc/systemd/system/parse.service`）：

```ini
[Service]
Type=simple
User=parse
Group=parse
WorkingDirectory=/opt/parse/app
EnvironmentFile=/opt/parse/.env
Environment=PATH=/opt/node22/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
ExecStart=/opt/node22/bin/node server.js
Restart=always
RestartSec=5s
# 与宿主机其他服务共享资源，按需限制
MemoryHigh=640M
MemoryMax=768M
CPUWeight=50
# 安全加固（注意：不要设 MemoryDenyWriteExecute —— V8 JIT 需要可写可执行内存）
NoNewPrivileges=true
PrivateTmp=true
PrivateDevices=true
ProtectSystem=full
ProtectHome=true
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true
RestrictSUIDSGID=true
RestrictRealtime=true
LockPersonality=true
ReadWritePaths=/opt/parse
```

```bash
systemctl daemon-reload && systemctl enable --now parse
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3000/api/health   # 期望 200
```

**与 Docker 路径的差异（必须知情）**：

| 项 | Docker | 无 Docker |
| --- | --- | --- |
| TikTok 解析 | 镜像内自带 `yt-dlp` + `ffmpeg` | **需自行在宿主机安装**，并在 `.env` 配 `YTDLP_BIN` |
| 滚动更新 | 自动强制更新时 Old/新容器混跑的 RSC 不一致问题 | 无此问题（进程整体重启），但需自己保证构建 ID 稳定（`NEXT_BUILD_ID`） |
| 依赖隔离 | 完全隔离 | 依赖宿主机；**Node 版本是硬约束**（§2.1） |
| 反向代理 / 限流 | 一般由前置层负责 | 同样建议前置 nginx（§七） |

> 如果服务器在**海外**，B 站会被 IP 风控（-412）：此时 `BILIBILI_RELAY_URL`（云函数中继）
> 或 `BILIBILI_PROXY` 必须配置，不能按"国内服务器可直连"处理。本次部署在国内，故二者留空。

### 8.4 回滚

**代码层回滚**（丢弃本地全部改造）：

```bash
git checkout -- .      # 丢弃全部工作区改动
git clean -fd          # 清理新增未跟踪文件（脚本、叠加层、文档）
```

**线上层回滚**（三种情形）：

| 情形 | 做法 |
| --- | --- |
| 自动部署健康检查失败 | **已经自动回滚**了。任务日志会打印失败原因与 `journalctl -u parse -n 40`，修好后重跑 |
| 部署成功但发现功能异常 | 服务器上 `/opt/parse/.backup.prev` 是上一个版本（成功部署后会删除，故通常不存在）。可靠做法是本地 `git revert` 那笔同步提交 → 推送 → 让流水线重新部署 |
| 需要立刻止血 | 在 GitHub 上禁用 `selfhost-sync` 工作流（Settings → Actions），线上保持当前版本不动 |

若只想**临时停用自动同步**，在 GitHub 上禁用 `selfhost-sync` 工作流即可，不影响已部署版本。
